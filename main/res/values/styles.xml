<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:buttonStyle">@style/Button</item>
        <item name="android:spinnerDropDownItemStyle">@style/Value</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base" parent="android:Widget.TextView">
        <!-- Customize your theme here. -->
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>
    <style name="Label" parent="Base">
        <!-- Customize your theme here. -->
        <item name="android:textColor">#0F7F12</item>
    </style>
    <style name="Value" parent="Base">
        <!-- Customize your theme here. -->
        <item name="android:textColor">#992F2F</item>
    </style>
    <style name="Result" parent="android:Widget.TextView">
        <!-- Customize your theme here. -->
        <item name="android:textColor">#303F9f</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>
    <style name="Button" parent="android:Widget.Button">
        <!-- Customize your theme here. -->
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_margin">4dp</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:background">@drawable/button</item>
    </style>
    <style name="DatePickerDialogStyle" parent="@style/Theme.AppCompat.Light.Dialog">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>
